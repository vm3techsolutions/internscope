export declare function calculateCountdownState(endDate: Date | number, startDate?: number | null): {
    timeRemaining: number;
    isComplete: boolean;
    progress: number | null;
    humanTimeRemaining: {
        days: string;
        hours: string;
        minutes: string;
        seconds: string;
        isComplete: boolean;
    };
};
